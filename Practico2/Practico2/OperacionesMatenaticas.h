#pragma once

double cal_square(double num1);
//take a numbers.
//calculate and return square of number.

double cal_addition(double num1, double num2);
//take two numbers.
//calculate and return addition of both.

double circle_area(double radius);
//take a radius value of cicle.
//return the area of cicle.

double maximal(double num1, double num2);
//take two numbers, if a equals numbers return this number.
//return the maximun of both.

void switchOM(char);
//represents switch for cases
// User can determine if perform calculate square of one number else perform calculate addition of two numbers
//else perform calculate area of cicle and finally else calculate maximal of two numbers
